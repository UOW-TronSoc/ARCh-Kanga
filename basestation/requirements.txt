fastapi==0.115.6
uvicorn==0.34.0
# WebSocket support for uvicorn (operator control/telemetry channels).
websockets==14.1
# Signed session cookies for PIN auth.
itsdangerous==2.2.0
# Safe parsing for browser-editable motor-limit YAML.
PyYAML==6.0.2
